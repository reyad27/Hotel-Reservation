spring:

  application:
    name: hotel-reservation

  datasource:
    url: jdbc:postgresql://localhost:5432/postgres?currentSchema=hotel_reservation
    username: postgres
    password: Ahmad1234
    driver-class-name: org.postgresql.Driver
  jpa:
    #this will create table if it doesn't exist and update if exist
    #and will replace it with validate soon, read about it and make sure what the difference
    hibernate:
      ddl-auto: update
    #this will show you what the query that fired
    show-sql: true